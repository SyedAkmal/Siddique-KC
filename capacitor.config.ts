import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.clone.swiggy',
  appName: 'Swiggy Clone',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
